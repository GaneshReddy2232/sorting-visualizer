# Sorting Visualizer — Neon Hacker (v2 UI)

- Flask (Python), server-rendered frames
- No JavaScript/Node
- Prebuilt Tailwind CSS included locally (static/tailwind.min.css)
- Control panel styled closer to the screenshot: dropdown, size/speed fields, transport controls, reset, step counter.

## Run
```bash
python -m venv .venv
# activate it, then:
pip install Flask
python app.py
# open http://127.0.0.1:5000/
```
